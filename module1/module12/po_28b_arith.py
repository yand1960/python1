import module1
import module1.module12.po_28b_arith


class Arith:

    def plus(self, x, y):
        return (x + y) * (1 - module1.tax)

    def minus(self, x, y):
        return (x - y) * (1 - module1.tax)